{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "기본 권한 — 메인 윈도우용",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "shell:allow-open"
  ]
}
